import 'package:flutter_test/flutter_test.dart';

// Remote-push tests removed in sync-overhaul patch 0001:
// repositories no longer push to remote inline — the sync engine owns all
// remote I/O. If new repository-level tests are needed they belong here.
void main() {
  group('MarkerRepositoryImpl', () {
    // placeholder — add tests here if repository-level behaviour is added
  });
}
